name: Mobile App Build (APK)

on:
  push:
    branches: [main]
    paths:
      - 'mobile-app/**'
      - '.github/workflows/mobile-build.yml'
  pull_request:
    branches: [main]
    paths:
      - 'mobile-app/**'
  workflow_dispatch:
    inputs:
      build_mode:
        description: "Build mode: cloud (EAS) or local (Gradle debug)"
        required: false
        type: choice
        options:
          - cloud
          - local
        default: cloud

permissions:
  contents: read

jobs:
  build-apk:
    name: Build Android APK (${{ github.event.inputs.build_mode || 'cloud' }})
    runs-on: ubuntu-latest
    env:
      REQUESTED_BUILD_MODE: ${{ github.event.inputs.build_mode || 'cloud' }}
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: mobile-app/package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Expo
        working-directory: mobile-app
        run: |
          npm ci
          CI=1 npx expo prebuild --platform android

      - name: Resolve build mode
        id: resolve_build_mode
        run: |
          if [[ "$REQUESTED_BUILD_MODE" == "cloud" && -n "$EXPO_TOKEN" ]]; then
            echo "mode=cloud" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "mode=local" >> "$GITHUB_OUTPUT"

          if [[ "$REQUESTED_BUILD_MODE" == "cloud" ]]; then
            echo "::warning::EXPO_TOKEN is not configured; falling back to local Gradle APK build."
          fi

      - name: Setup EAS CLI
        if: steps.resolve_build_mode.outputs.mode == 'cloud'
        run: npm install -g eas-cli

      - name: Build APK (EAS cloud)
        if: steps.resolve_build_mode.outputs.mode == 'cloud'
        working-directory: mobile-app
        run: |
          eas build --platform android --profile preview --non-interactive --wait --json > eas-build-result.json

      - name: Resolve cloud artifact URL
        if: steps.resolve_build_mode.outputs.mode == 'cloud'
        id: cloud_artifact
        working-directory: mobile-app
        run: |
          ARTIFACT_URL=$(node -e "const fs=require('fs');const data=JSON.parse(fs.readFileSync('eas-build-result.json','utf8'));console.log(data[0]?.artifacts?.buildUrl||'');")
          echo "url=$ARTIFACT_URL" >> "$GITHUB_OUTPUT"

      - name: Download cloud APK artifact
        if: steps.resolve_build_mode.outputs.mode == 'cloud' && steps.cloud_artifact.outputs.url != ''
        working-directory: mobile-app
        run: |
          mkdir -p artifacts
          curl -L "${{ steps.cloud_artifact.outputs.url }}" -o artifacts/adas-infotainment-cloud.apk

      - name: Build APK (local Gradle)
        if: steps.resolve_build_mode.outputs.mode == 'local'
        working-directory: mobile-app/android
        run: ./gradlew assembleRelease

      - name: Upload APK artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: adas-infotainment-apk
          path: |
            mobile-app/artifacts/*.apk
            mobile-app/android/app/build/outputs/apk/**/*.apk
          if-no-files-found: warn
